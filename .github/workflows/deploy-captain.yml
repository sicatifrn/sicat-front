name: Deploy Captain

on:
  push:
    branches: [main]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Captain build
        run: curl -fsS -X POST "$CAPROVER_WEBHOOK_URL"
        env:
          CAPROVER_WEBHOOK_URL: ${{ secrets.CAPROVER_WEBHOOK_URL }}
